export function renderFormattedText(text, selections) {
  if (!selections?.length) return text;

  try {
    // Format text logic...
  } catch (err) {
    console.error("Error rendering formatted text:", err);
    return text;
  }
}
